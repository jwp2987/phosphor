You are powered by the model named {{ model_id | default("unknown") }}.
Here is some useful information about the environment you are running in:
<env>
{%- if shell %}
  Shell: {{ shell.name }}{% if shell.version %} {{ shell.version }}{% endif %}
{%- endif %}
{%- if os %}
  Platform: {{ os.platform }}{% if os.distribution %} ({{ os.distribution }}){% endif %}
{%- endif %}
</env>
Live environment state (working directory, git branch, current date) is provided in an
<environment_context> block at the end of the conversation. Always trust that block over
any directory or branch mentioned earlier, including in older tool output.
