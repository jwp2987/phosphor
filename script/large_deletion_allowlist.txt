# Paths whose deletion does not count toward script/check_large_deletions.
#
# One bash glob per line; `#` comments and blank lines ignored. Matching is
# `[[ path == pattern ]]`, so `*` CROSSES `/` -- `docs/*` covers `docs/a/b.md`.
# A trailing `/` means "this directory and everything under it".
#
# THIS LIST MAY SHRINK FREELY. Adding to it is a claim that the path is not
# fork behaviour -- generated output, vendored third-party code, a lockfile,
# scratch -- and it needs the same scrutiny as any other boundary change
# (AGENTS.md 5.10). Every entry below carries the measurement that put it here;
# a new entry without one is a guess, and guesses are how a guard becomes
# decorative.
#
# Deliberately NOT here, and each omission is on purpose:
#
#   lib/rust-genai/    Vendored, but the fork patches it (the reqwest 0.13 bump
#                      lives there). Deleting it would be a real behaviour
#                      change and should have to say so.
#   app/assets/        Bundled SVG/text assets are counted. Binary blobs are
#                      already skipped by the numstat `-` check, and the text
#                      ones are product surface.
#   crates/*/tests/    Test deletion is exactly the shape AGENTS.md 5.6 forbids
#                      doing quietly.

# Prose and the generated ledgers. `93e738bb9` deletes a single 4,445-line
# markdown plan file; `docs/STATE.md` is regenerated by script/state, and
# docs/sweep-verdict-ledger.tsv (1,843 rows) is rewritten wholesale by
# script/extract_sweep_ledger.py. Neither is fork behaviour.
*.md
docs/

# Dependency lockfiles. Regenerated by the toolchain, never hand-edited, and
# routinely thousands of lines. `f2b127341` alone deletes a 4,173-line
# pnpm-lock.yaml. Cargo.lock churns on every dependency bump.
Cargo.lock
*.lock
*/*.lock
pnpm-lock.yaml
package-lock.json

# Agent scratch, tracked by accident and untracked again in `7b2526658`
# ("untrack .claude/jobs scratch", -7,008 lines across 14 files). Not code.
.claude/

# Build output. Present in the working tree, never a reviewable deletion.
target/
